#include "main.h"
/**
 * _abs - A function a print the absolute of a number
 * @num: to accept integer
 * Return:to return  num
 */
int _abs(int num)
{
	num = num;

	if (num < 0)
	{
		return (-num);
	}
	else
	{
		return (num);
	}
}
